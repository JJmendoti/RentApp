import {StyleSheet, Dimensions} from 'react-native';

const Styles = StyleSheet.create({
    container: {
      flex: 1
    },
    containersha: {
      marginTop: 10
    }
  });

export default Styles;